# Fixtures

Use this directory for tiny deterministic examples that support tests,
documentation snippets, and local development.

Current fixture:

- `support_tickets.jsonl`: synthetic support-ticket extraction rows with explicit
  `train` and `eval` splits for schema and manifest tests
