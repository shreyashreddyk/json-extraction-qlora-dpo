# Data Layout

This directory holds lightweight project data assets and local working folders.

- `manifests/` stores dataset manifests and provenance records.
- `fixtures/` stores tiny deterministic samples for tests and examples.
- `interim/` stores local preprocessing outputs.
- `eval/` stores local evaluation subsets and temporary prediction dumps.

Raw external datasets should stay out of Git.

